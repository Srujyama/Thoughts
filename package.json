{
  "name": "thoughts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run test && npm run build && npm audit --audit-level=high",
    "test": "node test/list-editing.test.mjs && node test/move-integrity.test.mjs && node test/contrast-check.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "dompurify": "^3.4.14",
    "firebase": "^12.15.0",
    "marked": "^18.0.9"
  }
}
