{
  "name": "thoughts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run build && npm audit --audit-level=high"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "dompurify": "^3.4.14",
    "firebase": "^12.15.0",
    "marked": "^18.0.9"
  }
}
