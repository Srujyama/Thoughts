rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Vault files live at {uid}/{path} — each user owns their prefix.
    match /{userId}/{allPaths=**} {
      allow read, write: if request.auth != null
                         && request.auth.uid == userId;
    }
  }
}
